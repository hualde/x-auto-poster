/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['x-auto-poster.vercel.app', 'vercel.app', 'public.blob.vercel-storage.com'],
  },
}

module.exports = nextConfig